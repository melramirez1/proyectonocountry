services:
  db:
    image: postgres:16
    container_name: mkre-db-nocountry
    restart: always
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=secret_password_123
      - POSTGRES_DB=nocountry_db
    ports:
      - "5432:5432"
